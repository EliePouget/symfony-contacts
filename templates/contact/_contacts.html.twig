<ul>
    {% for contact in contactList %}
        <li class="contacts">
            <a href="{{ path('app_contact_id', {id: contact.id}) }}">
                <span class="lastname">{{ contact.lastname }}</span>,
                <span class="firstname">{{ contact.firstname }}</span>
            </a>
        </li>
    {% endfor %}
</ul>